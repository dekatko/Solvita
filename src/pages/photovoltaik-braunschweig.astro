---
import { getEntry } from 'astro:content';
import OrtLayout from '../layouts/OrtLayout.astro';

const entry = await getEntry('orte', 'braunschweig');
if (!entry) throw new Error('Missing orte entry: braunschweig');
---

<OrtLayout entry={entry} routeSlug="photovoltaik-braunschweig" />
