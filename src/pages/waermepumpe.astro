---
import { getEntry } from 'astro:content';
import LeistungLayout from '../layouts/LeistungLayout.astro';

const entry = await getEntry('leistungen', 'waermepumpe');
if (!entry) throw new Error('Missing leistungen entry: waermepumpe');
---

<LeistungLayout entry={entry} />
