---
import { getEntry } from 'astro:content';
import OrtLayout from '../layouts/OrtLayout.astro';

const entry = await getEntry('orte', 'gifhorn');
if (!entry) throw new Error('Missing orte entry: gifhorn');
---

<OrtLayout entry={entry} routeSlug="photovoltaik-gifhorn" />
