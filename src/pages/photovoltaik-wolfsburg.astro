---
import { getEntry } from 'astro:content';
import OrtLayout from '../layouts/OrtLayout.astro';

const entry = await getEntry('orte', 'wolfsburg');
if (!entry) throw new Error('Missing orte entry: wolfsburg');
---

<OrtLayout entry={entry} routeSlug="photovoltaik-wolfsburg" />
